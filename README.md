# voucher-analysis

Journal entry testing (JET) and bank reconciliation on synthetic data.

The project is being rewritten with pandas. This README will be completed
when the rewrite is finished. The previous standard-library version is in
the git history (commit affab02).
